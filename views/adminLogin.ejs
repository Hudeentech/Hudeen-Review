<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1 style="text-align: left;">Admin Login</h1>
        <p style="text-align: left; color: var(--text-secondary);">Access the testimonial management panel.</p>
        <hr>
        <% if (message) { %>
            <div class="message error"><%= message %></div>
        <% } %>
        <form action="/admin/login" method="POST">
            <label for="username">Full Name</label>
            <input type="text" id="username" name="username" required placeholder="Please enter your admin username">

            <label for="password">Password</label>
            <input type="password" id="password" name="password" required placeholder="Please enter your admin password">

            <button  style="background: gold;color: #333;" type="submit">Login</button>
        </form>
        <div style="text-align: center; margin-top: 20px;">
            <a href="/" style="color: var(--accent-blue); text-decoration: none; font-weight: 600;">← Back to Landing Page (API Consumers)</a>
        </div>
    </div>
</body>
</html>
